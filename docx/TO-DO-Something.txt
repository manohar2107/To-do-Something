# 🚀 Project: To-Do Something (Modern Full-Stack Rebuild)

An ultra-lightweight, high-performance task management application migrated from legacy React architectures into a modern, context-driven frontend. This project is currently scaling into a secure, full-stack collaborative ecosystem.

---

## 🏛️ Project Evolution & Architectural Audit

### 1. The Legacy Base (Decommissioned)
Originally constructed using classical React paradigms:
* **Class Components & Lifecycle Methods:** Heavily reliant on class constructs and manual performance tuning.
* **The Binding Trap:** State updates required intensive constructor declarations and explicit function binding (`this.method.bind(this)`).
* **Prop-Drilling Architecture:** State manipulation was bound to parental components, forcing intermediate UI layouts to act as passive data couriers.
* **Direct State Mutation:** Task arrays were manipulated using invasive native methods (`splice`/`push`), creating tracking inefficiencies within the virtual DOM.

### 2. The Modern Core (Current State)
The frontend has been entirely stripped and refactored to conform to production-grade design principles:
* **Build Tooling:** Replaced Create React App (`react-scripts`) with **Vite** for near-instantaneous compilation and hot module reloading.
* **State Decoupling (`useReducer`):** Extracted business logic out of UI components into an isolated pure-function state machine (`todoReducer.js`).
* **Global State Broadcast (Context API):** Engineered a global broadcast station (`TodoProvider`) to entirely eliminate prop-drilling pipelines, enabling clean, direct consumer subscriptions via `useContext`.
* **Immutability Filters:** Rewrote row array rendering to safely utilize automated pipeline maps (`.filter().map()`), optimizing frame rendering behaviors.

---

## 🗺️ Engineering Roadmap

The project is structured across modular implementation milestones:

| Phase | Milestone Description | Tech Stack Components | Status |
| :--- | :--- | :--- | :--- |
| **Phase 1** | Local Environment & Bundler Migration | Vite, React 18, Native Shell | 🟩 Complete |
| **Phase 2** | State Machine & Context Injection | `useReducer`, `useContext`, Functional Hooks | 🟩 Complete |
| **Phase 3** | Authentication & Relational Storage | Node.js, Express, JWT (JSON Web Tokens), MongoDB/PostgreSQL | 🟨 Next Up |
| **Phase 4** | Real-Time Live Multi-User Sharing | WebSockets (`Socket.io`) / Bi-directional Sync | 🟦 Planned |

---

## ⚡ Local Setup and Execution

To run this environment locally, ensure you have **Node.js (v24+)** initialized, then execute:

```bash
# Install the modern, optimized dependencies
npm install

# Run the local development server
npm run dev

Phase 3: The Gateway — Node.js, Express, & Shared Database
To build your killer feature—allowing 2 or more users to view and modify the exact same to-do list simultaneously—we need a centralized supervisor. A browser's local storage can't talk to another browser across the world, but a backend server can.

Here is how our full-stack universe is going to be structured:

[ Client A (Vite App) ] ──(HTTP/WebSockets)──┐
                                              ▼
                                    [ Server (Node + Express) ] ──> [ Database ]
                                              ▲
[ Client B (Vite App) ] ──(HTTP/WebSockets)──┘
The Backend Strategy
To keep your project clean and contained in your single GitHub repository, we will create a parallel server workspace. Your project root will seamlessly handle both sides of the ball:

src/, public/, etc. -> Frontend Client (Vite)

server/ -> Backend API (Node.js + Express)


## 🌐 Production Cloud Architecture & CI/CD

The application is deployed across a decoupled multi-cloud architecture:

* **Frontend Hosting:** [Vercel](https://to-do-something-pearl.vercel.app) (Edge CDN with SPA fallbacks)
* **Backend API:** [Google Cloud Run](https://todo-something-714330462911.asia-south2.run.app) (Serverless containerized Express service in `asia-south2`)
* **Persistence:** [MongoDB Atlas](https://cloud.mongodb.com) (Multi-node M0 replica set)
* **Reverse Proxy:** Configured via `vercel.json` to proxy `/api/*` requests directly to Cloud Run, eliminating cross-origin preflight latency and CORS restrictions.

### Continuous Deployment Pipeline

Deployments follow a strict Git tag promotion workflow to prevent unstable code from leaking into production:

1. **Active Development:** All daily commits and feature experiments remain on `master`.
2. **Release Staging:** Stable milestones are merged from `master` into the `deploy` branch.
3. **Automated Builds:** Pushing an annotated tag matching `^v.*` (e.g., `git push origin v0.3.0`) triggers Google Cloud Build to:
   * Build the production Docker image using `server/Dockerfile`.
   * Push the container artifact to Google Artifact Registry.
   * Roll out a zero-downtime revision on Google Cloud Run.